export type ProjectItem = {
  title: string;
  description: string;
  image: string;
  altText: string;
  reverse?: boolean;
};


export type InfoCardItem = {
  letter: string;
  order: string;
  header: string;
  description: string;
  
}

export type faqItem = {
  question: string;
}